export default function Interface({score, highScore, level}) {

    return (
        <>
            <h2>Score: {score}</h2>
            <h2>high score: {highScore}</h2>
            <h2>Level: {level}</h2>
        </>
    )
}