export default function Interface({score, highScore, level}) {

    return (
        <>
            <h1>Score: {score}</h1>
            <h1>high score: {highScore}</h1>
            <h1>Level: {level}</h1>
        </>
    )
}